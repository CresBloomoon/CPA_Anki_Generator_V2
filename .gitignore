# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / frontend
node_modules/
dist/

# Secrets
.env
!.env.example

# Claude Code (個人のローカル設定)
.claude/settings.local.json

# Runtime-generated app config (created on first save; see
# SettingsRepository / RootPathHistoryRepository). Bind-mounted into the
# backend container as /app/data (see docker-compose.yml) so it survives
# container recreation.
backend/data/

# OS
.DS_Store
backend/tmp_samples/
