# CLAUDE.md

このファイルは、このリポジトリで作業する際にClaude Code（claude.ai/code）へガイダンスを提供するものです。

## コミュニケーションについて

常にユーザーとの対話は日本語で行うこと。

## プロジェクトの状態

このリポジトリは**実装着手前**の状態です。現時点では
`docs/specs/cpa-anki-generator-v2-implementation-request.md`（日本語）の実装依頼書のみが存在し、アプリケーション
コード・コミット・パッケージマニフェスト（`package.json`等）は一切ありません。作業を始める前に必ずこの依頼書を
通読してください。要件に関して権威を持つのはこの依頼書であり、本ファイルはコードベースを把握するために必要な
部分のみを要約したものです。存在しないビルド／テストコマンドを創作しないこと。該当するツール（Docker Compose、
`package.json`、`pyproject.toml`／`requirements.txt`）が実際に作成された時点で、このファイルに追記すること。

## これは何か

既存のStreamlit＋Gemini構成のアプリを完全に作り直したものです。公認会計士試験のテキストPDFをAnkiフラッシュ
カードデッキに変換します。新バージョンでは、Streamlit製フロントエンドをReactに置き換え、単一プロバイダー
（Gemini）依存だったAI連携を、プロバイダーを切り替え可能なストラテジーパターンへと一般化します。既存のカード
テンプレート・プロンプトの内容自体は踏襲します。

## 参照用リポジトリ（読み取り専用）

旧アプリは、別のワーキングディレクトリとして `/workspace/CPA_Anki_Generator` にチェックアウト済みです
（`--add-dir`経由で既に利用可能）。**このリポジトリは絶対に編集しないこと**。参照専用として、以下の目的にのみ
使用してください。

- `app/card_templates/` — 4段構成カードレイアウトの既存Front/Back HTML＋CSS。見た目の構成はそのまま移植する。
- `app/prompts/project_constitution.md` と `app/prompts/anki_generation.md` — 「役割・原則の指示」と「出力
  フォーマット契約」を分ける2ファイル構成の慣習。この方針は維持したまま引き継ぐ。
- `app/main.py` の `st.data_editor` の使い方 — これは**反面教師**として参照するものであり、パターンとして
  真似するものではない。これはStreamlitのキャッシュ不整合バグ（最初の編集でセルの内容がリバートする挙動）を、
  データフレームの `.copy()` を渡すことと、`sections_df` / `sections_df_latest` という2つの状態を並行して
  保持することで回避したものである。新しいReact製の節編集テーブルでは、この二重ステートの力技を再現しては
  いけない（意図している簡潔なアプローチについては下記アーキテクチャ節を参照）。

旧リポジトリ自身の`CLAUDE.md`には、そのパイプライン（`pdf_parser.scan_pdf_structure` → 節ごとのGemini呼び出し
→ `anki_creator.create_anki_package`）についてより詳細な記載があるため、旧アプリの挙動についてさらに深く知る
必要がある場合はそちらも参照すること。

## 目指すアーキテクチャ

依頼書の通り、バックエンドは厳格な4層構造（`CresBloomoon/CPA-Dashboard-Core`と同じ設計思想）に従います。新規
コードはすべてこの構成に厳密に従うこと。責務の分離はコメントではなく、関数・クラス・コンポーネントを細かく
分割することによって実現する。

```
routes/          # APIエンドポイント（FastAPI）
usecases/        # ビジネスロジック：構成スキャン、カード生成、パッケージング
repositories/
  ai/            # AiCardGeneratorRepository — ストラテジーパターン
  pdf/           # PDF解析
  anki/          # genankiラッパー
domain/          # エンティティ：Section, Card, Deckなど
```

**AIプロバイダーの抽象化**：`AiCardGeneratorRepository`は抽象基底クラスであり、
`generate_cards(section_text, prompt_context) -> CardContent`を持つ。具象実装
（`GeminiRepository`、`ClaudeRepository`、`ChatGptRepository`）はそれぞれ、各プロバイダー固有の構造化出力
強制方式（Geminiの`response_mime_type`、Claudeのtool use、GPTの`response_format`）の違いを内部で吸収し、
共通の`CardContent`を返す。モデルバージョン（例：`gemini-2.5`と`3.5`）は別クラスにせず、コンストラクタ引数で
表現する。UseCases層は、`.env`／`settings.json`から読み込んだプロバイダー・モデル設定をもとに、Factory経由で
リポジトリのインスタンスを取得する。プロンプト本文は当面すべてのプロバイダーで同一とする（`project_constitution.md`
＋`anki_generation.md`の1組のみ）。依頼がない限り、プロバイダー別のプロンプトのバリエーションを作らないこと。

**フロントエンドの節編集テーブル**：シンプルな`<table>`＋React stateのみとし、テーブルライブラリ（TanStack
Table等）は使わない（行数・パフォーマンスの観点で実際に必要になるまでは明示的に見送る）。列構成：チェック
ボックス／編集可能な節タイトル／編集可能な開始ページ／編集可能な出力デッキ名（自動補完あり）／編集可能な
最低カード枚数／ソースファイル。

**カードテンプレート**：既存の4段構成（論証／解説／要するに？／留意点）を維持する。「要するに？」セクションの
プロンプトでは、意図的にたとえ話を使うよう明記すること。これは監査論など実務未経験者が直感的なイメージを
持ちにくい分野の理解を助けるためである。`PAGE_CODE`（テキスト自体が持つページ参照コード、例：③-8-1）は、
AIが実際のPDFの内容から抽出する必要があり、すべてのカードに必須である。生成されたカード一覧は`PAGE_CODE`を
軸にソート・検索できる必要がある。

**パッケージング**：`genanki`による`.apkg`生成は、既存リポジトリのやり方を踏襲する。

## 技術スタック

- フロントエンド：React (Vite) + TypeScript + Tailwind CSS
- バックエンド：FastAPI (Python)、PDF解析にPyMuPDFを使用
- 開発・デプロイ：frontend／backendのコンテナを分離したDocker Compose構成（開発者の意向により、ホストOSへの
  直接インストールは避けること。実行が必要なものはコンテナ化されたワークフローを優先する）
- シークレット：プロバイダーごとに1本のAPIキーを`.env`で管理する（`GEMINI_API_KEY`／`CLAUDE_API_KEY`／
  `OPENAI_API_KEY`）。複数キーによるフォールバックは行わない（旧リポジトリの`GEMINI_API_KEY_1〜3`による
  ローテーション機構は、今回は明示的に引き継がない）。キー名のみを記載した`.env.example`を用意すること。

## 明示的にスコープ外とするもの

- 認証機能・ユーザー管理、複数ユーザー対応（Tailnet経由の単一ユーザー利用を前提とするため不要）
- 他アプリ（study-tracker-app等）との連携
- Ankiへの自動インポート・自動同期（AnkiWebには公開APIが存在せず、AnkiConnectはAnki Desktop限定のため）。
  手動でのSync操作を前提とする

## 依頼書に基づく開発者の作業スタイルについて

- 依頼者はC#でのテスト駆動開発・ドメイン駆動設計の実務経験が5年あるが、TypeScript／React／FastAPIについては
  初心者である。コメントによる説明よりも、責務の分割による可読性の高さを優先すること。抽象化を凝らすよりも、
  明示的であることを優先すること。
- 推測によるデバッグを行わないこと。エラーが発生した場合は、原因特定に必要な実際のログ・ファイル・実行環境の
  情報を確認したうえで対処すること。
- 依頼書が示す実装順序（ドメイン層 → repositories層 → usecases層 → routes層 → フロントエンドのアップロード／
  編集フロー → フロントエンドの生成／ダウンロードフロー）の各ステップを実装したあとは、何を実装したか、
  どのファイルがどの責務を持つか、その変更にADRが必要かどうかを簡潔に説明すること。

## 実装依頼時の進め方

Phase単位の実装依頼を受けた際は、設計合意事項をそのまま実装に着手するのではなく、以下の順序で進めること：

1. 現状把握：関連ファイルを実際に読み、計画時点（設計合意時点）の想定と、実装済みコードの実態にズレがないかを
   確認する
2. Todoリスト作成：設計合意事項に加え、現状把握で見つかった関連論点（指定範囲外だが対応が必要な見落とし等）も
   含めて整理する
3. 実装着手前に、Todoリストをまーくんに提示し、確認を得る

この順序により、Phase4-8/5-21では指定範囲外だが重要な見落とし（DownloadButton.tsxのdoneCount問題）を、
Phase5-22ではApp.tsx側のdoneCount検出条件とDownloadButton側の実装のズレ（PARTIALLY_DONEの扱いの不整合）を、
それぞれ実装着手前に発見できた。この進め方を今後のPhaseでも標準の運用として継続する。

## 開発ログ運用ルール

- 各Phase完了時、`docs/dev-logs/` 配下に `phase{番号}-{短い説明}.md`（例：
  `docs/dev-logs/phase1-1-section-domain.md`）という形式でログファイルを作成する。
- ログに含める内容：
  - 何を実装したか（対象ファイルと、それぞれが持つ責務の要約）
  - 実装中に発見したバグ・問題点があれば、その内容と対処方法
  - 大きな判断（設計選択・トレードオフ）をした場合は、その理由
  - ADRを書いた場合は、そのファイルへのリンク
- 作成タイミング：そのPhaseのdiffを提示し、ユーザーの承認を得てコミットした**後**に作成する。
  ログファイル自体も（後追いの別コミットではなく）そのPhaseのコミットに含める。

### Phaseに紐づかない軽微な変更について

`.gitignore`調整・typo修正など、特定のPhaseに紐づかない軽微な変更は上記のログ運用ルールの対象外とする。

- `docs/dev-logs/`への記録は不要。
- コミットメッセージは`feat:`／`fix:`／`chore:`／`docs:`のいずれかのプレフィックスで統一する
  （例：`chore: .gitignoreに.claude/settings.local.jsonを追加`）。
- Phase番号を冠したコミット（例：`Phase1-1: ドメイン層 Sectionを追加`）は従来通りとし、
  このプレフィックス規約の対象外。

## サンドボックスで検証できない依存ライブラリの扱い

依存ライブラリの実際の挙動がサンドボックス内で確認できず、その不確実性がテストの
assert（期待値）の正しさに直結する場合、推測でテストを書いたり削除したりしない。
以下の手順を踏む：

1. 検証したい内容を最小限に絞った調査スクリプトを`backend/tmp_samples/`に作成する
   （このディレクトリは`.gitignore`対象のため、リポジトリを汚さない）
2. まーくんに実行を依頼し、Docker環境での実行結果を確認する
3. 結果を踏まえてテスト・実装の方針を確定する
4. 役目を終えた調査スクリプトは削除する

一方、以下のようなケースは今まで通りクロコの判断で進めてよい（都度確認を挟まない）：
- 構文チェックのみで完結する確認（`py_compile`等）
- サンドボックス内でimport・実行が完結する純粋ロジックの検証
- ライブラリの挙動に依存しない、コードの構造・分割自体に関する判断

## Docker確認の運用

まーくんにDocker環境での動作確認を依頼する際は、変更内容によらず毎回
`docker compose build backend` → `pytest`実行、の順で統一する。

理由：新規テストファイル追加後に`docker compose build backend`を挟まず
`docker compose run --rm backend`だけでpytestを実行したところ、新規ファイルが
テスト収集に出てこず、件数が更新されないまま「全件パス」に見えてしまう事象が
実際に発生した（イメージ内のソースが古いまま実行されていたため）。「今回は
ビルドが要るかどうか」を都度判断するコストと見落としのリスクを避けるため、
変更の大小に関わらず常にビルドから行う。
